# 중개사무소 개업·폐업 현황판

전국 부동산 중개사무소의 월별 개업·폐업 추이를 보여주는 정적 웹 대시보드입니다. 서버 없이 GitHub Pages에 무료로 배포할 수 있습니다.

## 폴더 구성

```
├── index.html            # 대시보드 (data.json을 읽어 차트 표시)
├── aggregate.py          # CSV → data.json 집계 스크립트
├── data.json             # 집계 결과 (처음엔 샘플 데이터)
├── snapshots/            # 매월 다운로드한 원본 CSV를 넣는 곳
└── .github/workflows/    # push하면 자동으로 집계 + 배포
```

## 처음 한 번만 하는 설정

1. GitHub에 새 저장소를 만들고 이 폴더 전체를 push합니다.
2. 저장소 Settings → Pages → Source를 **GitHub Actions**로 선택합니다.
3. push하면 자동으로 배포됩니다. 주소는 `https://<아이디>.github.io/<저장소명>/`

로컬에서 미리 보려면 이 폴더에서 `python -m http.server` 실행 후 http://localhost:8000 접속.
(index.html을 더블클릭으로 열어도 내장 샘플 데이터로 화면은 확인할 수 있습니다.)

## 매월 하는 일 (5분)

1. 브이월드 공간정보 다운로드에서 **부동산중개업** 전국 CSV를 다운로드합니다.
   - https://www.vworld.kr/dtmk/dtmk_ntads_s002.do?dsId=11 (로그인 필요)
   - 또는 공공데이터포털의 "전국공인중개사사무소 표준데이터" / "국토교통부 부동산중개업정보"
2. 파일명을 `YYYY-MM.csv`로 바꿔 `snapshots/`에 넣습니다. 예: `snapshots/2026-07.csv`
3. `python aggregate.py` 실행 → `data.json` 갱신 확인
4. commit & push → 자동 배포

## 집계 방식 (중요)

- **개업**: 각 사무소의 `개설등록일자`를 월 단위로 셉니다. 모든 스냅샷을 합산해 집계하므로 스냅샷이 쌓일수록 과거 수치도 정확해집니다.
- **폐업**: 연속된 두 스냅샷을 비교해, 이전 달 파일에 있던 등록번호가 사라졌거나 영업상태가 '폐업'으로 바뀐 경우를 그 달의 폐업으로 셉니다.
  - 따라서 **스냅샷이 2개 이상 쌓여야 폐업 수치가 나오기 시작**합니다. 첫 달에는 폐업 그래프가 비어 있는 게 정상입니다.
  - 과거 폐업 추이까지 채우고 싶다면 한국공인중개사협회가 발표하는 월별 개업·폐업 통계를 수동으로 data.json에 넣는 방법도 있습니다.
- 이 방식은 데이터 반영 시차 때문에 공식 통계와 수치가 다소 다를 수 있습니다.

## 컬럼명이 달라서 오류가 날 때

데이터 출처에 따라 컬럼명이 다릅니다(예: `개설등록번호` vs `등록번호`). `aggregate.py` 실행 시 컬럼을 못 찾으면 실제 컬럼 목록을 출력해 주므로, 파일 상단의 `*_CANDIDATES` 리스트에 해당 이름을 추가하면 됩니다.

## 다음 단계 아이디어

- 시군구 단위 필터 추가 (주소에서 두 번째 토큰 파싱)
- 시도별 순증감 지도 시각화 (브이월드 지도 API 활용)
- 스냅샷 다운로드 자동화 (공공데이터포털 OpenAPI 키 발급 후 Actions 스케줄 실행)
